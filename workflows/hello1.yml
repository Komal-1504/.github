name: hello

on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  greet:
    
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "hello adosto"

      - name: Print User
        run: echo "hello $USER"
